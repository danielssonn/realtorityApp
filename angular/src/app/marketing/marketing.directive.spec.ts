import { MarketingDirective } from './marketing.directive';

describe('MarketingDirective', () => {
  it('should create an instance', () => {
    const directive = new MarketingDirective();
    expect(directive).toBeTruthy();
  });
});
